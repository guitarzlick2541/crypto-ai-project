# 🚀 Crypto AI Prediction Dashboard

ระบบพยากรณ์ราคา Cryptocurrency อัจฉริยะด้วย AI (Deep Learning LSTM) ที่มาพร้อมกับหน้า Dashboard แสดงผลแบบเรียลไทม์ ดีไซน์สวยงามระดับมืออาชีพ

![Project Status](https://img.shields.io/badge/Status-Active-success)
![Python](https://img.shields.io/badge/Python-3.12-blue)
![FastAPI](https://img.shields.io/badge/Backend-FastAPI-009688)
![TensorFlow](https://img.shields.io/badge/AI-TensorFlow-orange)

## 🌟 ฟีเจอร์หลัก (Key Features)

*   **🧠 Advanced AI Model:** ใช้โมเดล **LSTM (Long Short-Term Memory)** ที่มีความซับซ้อน รองรับข้อมูลเข้าถึง **15 ตัวแปร (Multi-features)** เช่น RSI, MACD, Volume, Bollinger Bands เพื่อความแม่นยำสูงสุด
*   **⏱️ Multi-Timeframe Support:** รองรับการทำนายทั้งระยะสั้น (5 นาที), ระยะกลาง (1 ชั่วโมง), และระยะยาว (4 ชั่วโมง)
*   **📊 Interactive Dashboard:**
    *   กราฟราคาแบบโต้ตอบได้ (Interactive Chart) ขับเคลื่อนด้วย **Chart.js**
    *   เส้นแสดงราคาจริง (Actual) เทียบกับราคาที่ AI ทำนาย (Predicted)
    *   ตัวบ่งชี้เทรนด์ (Trend Indicator) ที่บอกสถานะตลาดทันที (Uptrend/Downtrend)
*   **🔄 Auto-Sync System:** ระบบ **Scheduler** เบื้องหลังที่คอยดึงข้อมูลจาก Binance และอัปเดตผลทำนายให้โดยอัตโนมัติ (ไม่ต้องกด Refresh เอง)
*   **📈 Performance Tracking:** แสดงค่าความแม่นยำของโมเดลด้วยตัวชี้วัดมาตรฐานสากล (MAE, RMSE)
*   **⚙️ Modern Settings:** หน้าตั้งค่าที่ออกแบบใหม่ สวยงาม แบ่งหมวดหมู่ชัดเจน พร้อม Dark Mode
*   **💾 Historical Data:** ดูและตรวจสอบข้อมูลราคาย้อนหลังได้ในรูปแบบตาราง

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

### Backend (Core System)
*   **Python:** ภาษาหลักในการพัฒนา
*   **FastAPI:** สร้าง API ประสิทธิภาพสูง รองรับ Asynchronous
*   **TensorFlow / Keras:** สร้างและเทรนโมเดล Neural Network
*   **APScheduler:** จัดการงานอัตโนมัติตามเวลา (Cron Jobs)
*   **SQLite:** ฐานข้อมูลน้ำหนักเบา บันทึกประวัติการทำนาย
*   **Pandas / NumPy / Scikit-learn:** จัดการและประมวลผลข้อมูล (Data Science)

### Frontend (User Interface)
*   **HTML5 / CSS3:** สร้าง UI ที่ทันสมัยด้วย Glassmorphism Design
*   **JavaScript (Vanilla):** จัดการ Logic ฝั่งไคลเอนต์ เชื่อมต่อ API อย่างรวดเร็ว
*   **Chart.js:** Library สำหรับวาดกราฟที่สวยงาม

## 📦 การติดตั้งและการใช้งาน (Installation)

1.  **เตรียมสภาพแวดล้อม:**
    ```bash
    # สร้าง Virtual Environment
    python -m venv .venv
    
    # เปิดใช้งาน (Windows)
    .venv\Scripts\activate
    ```

2.  **ติดตั้ง Library ที่จำเป็น:**
    ```bash
    pip install -r requirements.txt
    ```

3.  **เริ่มการทำงาน (Run Application):**
    ```bash
    # สั่งรันด้วย Script ง่ายๆ เพียงคำสั่งเดียว
    python run.py
    ```
    *ระบบจะเปิด Web Browser และ Database ให้อัตโนมัติที่ `http://127.0.0.1:8000`*

## 📂 โครงสร้างโปรเจค (Project Structure)

```
crypto-ai-project/
├── backend/
│   ├── models/             # 🧠 ไฟล์โมเดล AI (.keras) และตัวแปลงข้อมูล (.pkl)
│   ├── tests/              # 🧪 ไฟล์ Unit Test
│   ├── ai_engine.py        # โค้ดหลักส่วน AI (Load Model, Scaler, Predict)
│   ├── data_service.py     # เชื่อมต่อ Binance API ดึงข้อมูลกราฟและคำนวณ Indicator
│   ├── main.py             # หัวใจหลักของ API (FastAPI Routes)
│   ├── scheduler.py        # ระบบตั้งเวลาทำงานอัตโนมัติ
│   ├── db.py               # จัดการ Database
│   └── train_lstm_*.py     # สคริปต์สำหรับเทรนโมเดลใหม่ในแต่ละ Timeframe
├── frontend/
│   ├── index.html          # หน้าเว็บหลัก
│   ├── style.css           # ดีไซน์หลัก
│   ├── settings.css        # ดีไซน์หน้าตั้งค่า
│   └── script.js           # การทำงานฝั่งหน้าเว็บ
├── run.py                  # สคริปต์เริ่มต้นโปรแกรม (Launcher)
└── requirements.txt        # รายชื่อ Dependencies
```

## 🧪 การทดสอบระบบ (Testing)
ระบบรองรับการทดสอบด้วย `pytest` เพื่อความมั่นใจในความถูกต้องของ API
```bash
pytest backend/tests/test_main.py
```

## 🔄 การเทรนโมเดลใหม่ (Retraining)
หากต้องการอัปเดตโมเดลให้ฉลาดขึ้นด้วยข้อมูลล่าสุด:
1.  เข้าไปที่โฟลเดอร์ backend: `cd backend`
2.  รันสคริปต์เทรนตาม Timeframe ที่ต้องการ เช่น:
    ```bash
    python train_lstm_1h.py
    ```
    *ระบบจะทำการดึงข้อมูลใหม่, เทรน AI, และบันทึกไฟล์โมเดลทับตัวเดิมให้ทันที*

---
© 2026 Crypto AI Project. พัฒนาเพื่อการเรียนรู้และการใช้งานส่วนบุคคล
